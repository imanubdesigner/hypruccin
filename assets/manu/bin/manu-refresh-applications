#!/bin/bash

# manu:summary=Ensure all default .desktop, web apps, TUIs, and npx wrappers are installed.

mkdir -p ~/.local/share/icons/hicolor/48x48/apps/
cp ~/.local/share/manu/applications/icons/*.png ~/.local/share/icons/hicolor/48x48/apps/
gtk-update-icon-cache ~/.local/share/icons/hicolor &>/dev/null

# Copy .desktop declarations
mkdir -p ~/.local/share/applications
cp ~/.local/share/manu/applications/*.desktop ~/.local/share/applications/
cp ~/.local/share/manu/applications/hidden/*.desktop ~/.local/share/applications/

if manu-cmd-present foot; then
  cp ~/.local/share/manu/default/foot/foot.desktop ~/.local/share/applications/
fi

# Refresh the webapps, TUIs, and npx wrappers
bash $MANU_PATH/install/packaging/icons.sh
bash $MANU_PATH/install/packaging/webapps.sh
bash $MANU_PATH/install/packaging/tuis.sh
bash $MANU_PATH/install/packaging/npx.sh

update-desktop-database ~/.local/share/applications
