#!/bin/bash

# manu:summary=Install the ONCE service, enable its background service, and launch the TUI.
# manu:requires-sudo=true

echo "Installing ONCE..."
manu-pkg-add once-bin

echo "Enabling ONCE background service..."
sudo systemctl enable --now once-background.service

echo -e "\nLaunching ONCE..."
once
