#!/bin/bash

# manu:summary=Apply the current theme keyboard color to ASUS ROG keyboards
# manu:hidden=true

ASUSCTL_THEME=~/.config/manu/current/theme/keyboard.rgb

if manu-cmd-present asusctl; then
  asusctl aura effect static -c $(sed 's/^#//' $ASUSCTL_THEME)
fi
